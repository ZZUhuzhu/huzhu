<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:clickable="true"
    android:focusable="true"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_vertical">
    <ImageView
        android:id="@+id/search_history_item_image_view"
        android:contentDescription="@string/app_name"
        android:layout_width="20dp"
        android:layout_height="30dp"
        android:layout_marginTop="@dimen/common_margin_normal"
        android:layout_marginStart="@dimen/common_margin_large"
        android:layout_marginBottom="@dimen/common_margin_normal"
        android:layout_marginEnd="@dimen/common_margin_normal"
        android:scaleType="fitCenter"
        android:src="@drawable/search_btn_associate_history"/>
    <TextView
        android:id="@+id/search_history_item_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/common_margin_normal"
        android:textSize="@dimen/common_text_size_normal"
        android:text="@string/solider"/>
</LinearLayout>